#!/bin/bash
# 切換到本腳本所在的目錄
cd "$(dirname "$0")"
echo "=================================================="
echo "    正在啟動影片轉檔器本機伺服器..."
echo "=================================================="
echo "請勿關閉此視窗，關閉視窗將會停止伺服器運作。"
echo "打開瀏覽器並前往： http://localhost:8000"
echo "=================================================="
python3 server.py
